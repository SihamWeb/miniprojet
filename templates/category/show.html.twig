{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    <h1 class="text-center mb-5 mt-3">{{ category.name | upper }}</h1>

<div class="d-flex p-2 bd-highlight justify-content-center">
    <table class="table table-sm table-striped table-hover mx-5 border rounded border-dark border-5">
        <tbody>
            <tr class="table-primary">
                <th>Identifiant</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr class="table-primary">
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>
</div>
<div class="d-flex p-2 bd-highlight justify-content-center">
    <a class="btn btn-dark" href="{{ path('category_edit', {'id': category.id}) }}">Modifier</a>
</div>
<div class="d-flex p-2 bd-highlight justify-content-center mt-3">
    <a class="btn btn-outline-primary me-2" href="{{ path('category_index') }}">Retour à la liste</a>
    {{ include('category/_delete_form.html.twig') }}
</div>
{% endblock %}
